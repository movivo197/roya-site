# گذاشتن رؤیا روی سرور

اعلان فقط روی **https** (یا localhost) کار می‌کند. روی http معمولی مرورگر اجازه نمی‌دهد.

## ۱. فایل‌ها

کل همین پوشهٔ پروژه را روی سرور بگذارید (با `node_modules` لازم نیست). Node باید ۲۲ باشد.

## ۲. ساخت و اجرا

```bash
cd /مسیر/پروژه
npm install
npm run build:server
PORT=3000 HOST=0.0.0.0 node .output/server/index.mjs
```

سایت روی پورت ۳۰۰۰ بالا می‌آید. جلویش یک https بگذارید (Caddy یا nginx) و دامنه را به همین پورت برسانید.

نمونهٔ Caddy:

```
roya.example.com {
  reverse_proxy 127.0.0.1:3000
}
```

## ۳. اعلان

با اولین «روشن کردن اعلان» این دو فایل ساخته می‌شوند. پاکشان نکنید وگرنه اشتراک‌ها می‌سوزد:

- `data/vapid.json` کلید اعلان
- `data/push-subscriptions.json` دستگاه‌هایی که اجازه داده‌اند

فرستادن یک اعلان به همهٔ همان دستگاه‌ها، از همان پوشهٔ پروژه:

```bash
node scripts/push-send.mjs "رؤیا" "اگر خوابی مانده، همین حالا بنویسید."
```

یادآوری هر صبح، ساعت ۷ به وقت سرور:

```bash
0 7 * * * cd /مسیر/پروژه && /usr/bin/node scripts/push-send.mjs "رؤیا" "اگر خوابی مانده، همین حالا بنویسید."
```

در آیفون، کاربر باید اول سایت را به صفحهٔ خانه اضافه کند و از همان آیکون باز کند. بعد دکمهٔ اعلان کار می‌کند.
